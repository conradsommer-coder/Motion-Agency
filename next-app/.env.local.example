# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# Gemini API key — used server-side only via /api/gemini route
GEMINI_API_KEY=your_gemini_api_key_here
